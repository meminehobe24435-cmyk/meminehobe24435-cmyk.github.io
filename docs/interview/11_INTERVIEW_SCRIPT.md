# 11 面试口述稿（Interview Script）

> 四套不同时长的口述稿，按「站点门面 → 代码仓库 → 报告深度」三层结构展开。括注【】为提示动作/停顿，正式说时不读出来。

## 0. 通用开场锚点（每套都用）

> 「我是尤译庆，2027 届，嵌入式方向，实习在深圳维特智能。这是我搭的 GitHub Pages 作品集，把项目做成视频和报告，方便您直接看。」

---

## 1. 30 秒版（电梯陈述）

尤译庆，2027 届嵌入式开发实习生方向，实习于深圳维特智能。我用 GitHub Pages 做了一个作品集站点，集中展示 4 个嵌入式项目——双目视觉处理系统、CH585 三模无线鼠标、自瞄装置、目标追踪系统，配 2 个演示视频和 3 份 PDF 报告，线上地址是 `https://meminehobe24435-cmyk.github.io/`。站点纯 HTML+CSS、零依赖、HTTPS 直接发布；我的代码在另外几个仓库，比如实习做的 ESP32-S3 WiFi 记录仪和无人机 STM32 四轮小车。

---

## 2. 1 分钟版

您好，我是尤译庆，2027 届，嵌入式开发实习生方向，在深圳维特智能实习。

我做了一个 GitHub Pages 作品集，把 4 个项目做成可视化页面：首页就是「4 个项目 / 2 个演示视频 / 3 份报告」，HR 点开就能看。站点本身是纯 HTML5 + CSS、无 JS、无框架，走 GitHub Pages 直接发布 `main` 分支，用 `.nojekyll` 保证视频和 PDF 原样输出，上线后是 HTTPS。

内容上，最重的是「双目视觉处理系统」，有演示视频 + 66 页完整报告；还有「CH585 三模无线鼠标」的视频演示，我用 H.265 编码把视频压到 11.5 MB，比 H.264 省约 40% 码率。

我的代码主要在另外几个仓库：实习做的 ESP32-S3 WiFi 数据记录仪（`esp-recorder`）、无人机+STM32 四轮小车（`uav-car-adhesion-platform`）、杰理蓝牙工具集（`ac632n-dev-tools`）。作品集是门面，代码在这些仓库里，您想看哪块我都能现场打开讲。

---

## 3. 3 分钟版

**（0:00-0:30）定位 + 站点是什么**
我是尤译庆，2027 届，嵌入式开发实习生方向，实习在深圳维特智能。这是我的作品集站点 `https://meminehobe24435-cmyk.github.io/`，把嵌入式项目做成「能点开看的」视频和报告，而不是简历上的几行字。

**（0:30-1:20）技术实现（体现 Web 基本功）**
站点是纯静态：一个 `index.html`（282 行）+ 一份 `styles.css`（526 行），无 JavaScript、无框架、无构建。用 HTML5 语义化标签组织 6 个区块，CSS 变量做暗色主题、Grid 做响应式、`clamp()` 做流式字号。视频用 `preload="metadata"`，首屏只加载约 21 KB，不把 38.5 MB 视频拖进首屏；外链统一 `target="_blank" rel="noreferrer"` 防 reverse tabnabbing。

**（1:20-2:20）内容 = 4 个项目**
① 双目视觉处理系统：视频 + 66 页报告，featured 置顶；② CH585 三模无线鼠标：视频，H.265 编码；③ 自瞄装置、④ 目标追踪系统：各有一份报告。这里我如实说：站点的项目目前只有视频和报告，代码在别的仓库。

**（2:20-3:00）代码仓库 + 收尾**
我的代码证据在：`esp-recorder`（实习的 ESP32-S3 WiFi 记录仪，ESP-IDF + FreeRTOS + Flutter）、`uav-car-adhesion-platform`（STM32F103 四轮小车 + PX4 飞控覆盖层）、`ac632n-dev-tools`（杰理蓝牙工具链）。站点 6 月搭好、这些仓库 8 月整理上线，还没把链接补进作品集——这是我下一步要做的。您想深入哪块，我直接打开讲。

---

## 4. 8~10 分钟版（边演示边讲）

> 结构：**打开站点 → 逐区块演示 → 讲技术细节 → 切到代码仓库 → 讲报告深度 → 收尾总结**。

**（0:00-1:00）开场 + 打开站点**
我是尤译庆，2027 届，嵌入式方向，深圳维特智能实习。现在我打开作品集 `https://meminehobe24435-cmyk.github.io/`——首屏就是定位：Embedded Systems Portfolio，标题、一句话介绍、两个按钮、还有「4 项目 / 2 视频 / 3 报告」的数字。

**（1:00-2:00）首屏 + 技术栈（点开讲）**
先讲它是什么：纯 HTML5 + CSS，无 JS。为什么无 JS？因为跳转、播放、发邮件都是浏览器原生能力，我不需要为这些引入框架。我按 F12 打开 Network 给您看——首屏只请求 `index.html`（11.9 KB）和 `styles.css`（9.7 KB），两个视频是 `preload="metadata"` 只取元数据，PDF 完全不加载，所以首屏很轻。

**（2:00-4:30）项目区块（重点，边点边讲）**
点「查看项目」滚到项目区。第一个是 featured 的「双目视觉处理系统」，点「观看视频」——视频能直接播，时长约 3 分半，H.264 编码。再点「查看报告」，新标签打开 66 页 PDF，这是我的完整项目报告。
第二个是「CH585 三模无线鼠标」，这段视频我用 H.265（HEVC）编码，只有 11.5 MB，比同样画质的 H.264 省约 40% 码率——代价是 Firefox 等环境可能播不了，需要备源，这是我记得的兼容性点。
第三、四个是自瞄装置和目标追踪，各有一份报告，但**没有演示视频**，所以用了占位块——这是我内容上的一个缺口，如实说明。

**（4:30-6:30）切到代码仓库（把门面落到代码）**
这里我要主动讲：站点上的项目目前只有视频和报告，代码在别的仓库。我现在打开 `esp-recorder`——这是我在维特智能实习做的 ESP32-S3 WiFi 数据记录仪，ESP-IDF + FreeRTOS，AP+Station 混合模式、双 UART 透传、SD 记录、USB CDC/MSC 复合设备，还有 Flutter App。再看 `uav-car-adhesion-platform`——STM32F103 四舵机四电机小车 + PX4 飞控覆盖层，README 里明确写了「小车 PS2 链路已实物验证、飞控待联调」的证据边界。

**（6:30-8:00）报告深度 + 奖项**
回到站点，看技能区：C/C++、FreeRTOS、UART/SPI/I2C、LwIP、PID、FPGA——这些我都能对应到代码或报告。奖项区有国家一等奖、TI 杯省一、瑞萨杯等 8 项。联系区是邮箱/电话/微信。

**（8:00-收尾）总结**
总结：这个作品集是「三层结构」——站点讲可视化门面、仓库讲代码实现、PDF 讲项目深度；它的不足是「门面还没链接到代码」，这是我下一步要补的。整个站点零依赖、首屏轻、HTTPS 上线，这本身也体现我做工程的取舍能力。谢谢，您想深入哪一块？
